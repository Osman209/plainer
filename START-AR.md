# تشغيل Plainer

## إمتى أحتاج أشغّل السيرفر؟

- من موقع GitHub Pages: رفع PDF وWord وTXT يعمل داخل المتصفح؛ لا تحتاج تشغيل سيرفر على جهازك أو إدخال مفتاح.
- من نسخة على جهازك: شغّل `npm start` وافتح `http://127.0.0.1:3000` لقراءة PDF؛ لا تفتح ملف `index.html` مباشرة.
- المراجعة المباشرة بمفتاح ChatGPT أو Claude تحتاج السيرفر وإعداد المفتاح واسم النموذج.
- المراجعة بدون مفتاح متاحة على الموقع بالنسخ واللصق.

لإعادة تشغيل نسخة سبق إعدادها، افتح مجلد المشروع في الطرفية ونفّذ `npm start` فقط. لا تحتاج إعادة تثبيت المكتبات أو إنشاء ملف `.env` من جديد.

## بدون مفتاح أو خادم

اختَر العربية أعلى الصفحة، ثم اختَر «ChatGPT — بدون مفتاح» أو «Claude — بدون مفتاح».
اكتب النص واختَر نوع المراجعة واضغط زر البدء.
انسخ الطلب المعروض إلى الخدمة المختارة، ثم انسخ رد JSON كاملًا إلى خانة الرد في Plainer واضغط «استيراد الرد».
تظهر التعديلات في الواجهة الحالية لتقبلها أو ترفضها.
أبقِ صفحة Plainer مفتوحة أثناء التبادل. النصوص الطويلة والتحكيم والمناظرة قد تحتاج عدة طلبات متتالية.

## تشغيل OpenAI أو Claude على اللاب

1. ثبّت Node.js إصدار 22 أو أحدث.
2. فك الضغط، وافتح PowerShell داخل مجلد المشروع.
3. نفّذ:

```powershell
npm install
Copy-Item .env.example .env
notepad .env
```

4. ضع مفتاح المزود الذي تريد استخدامه في الملف محليًا.
   - OpenAI يحتاج `OPENAI_API_KEY` و`OPENAI_MODEL`؛ اسم النموذج تختاره من النماذج المتاحة لحساب API الخاص بك.
   - Claude يحتاج `ANTHROPIC_API_KEY`؛ اسم النموذج موجود في `ANTHROPIC_MODEL` ويمكن تغييره.
   - اترك مفتاح المزود الآخر فارغًا إن لم تستخدمه.
5. احفظ الملف، ثم نفّذ:

```powershell
npm start
```

6. افتح http://127.0.0.1:3000 واختَر المزود من أعلى الصفحة.

الخادم يعمل على جهازك ولا يحتاج اشتراك استضافة. طلبات النموذج نفسها لها تكلفة لدى المزود.
المفاتيح تبقى في ملف `.env` على جهازك ولا تُحفظ في المتصفح. لا ترفع هذا الملف إلى GitHub ولا ترسله في المحادثة.
أعد تشغيل الخادم بعد تعديل المفاتيح أو اسم النموذج.

## موقع GitHub Pages

ارفع ملفات المشروع المعدلة، بما فيها `index.html` و`mammoth.browser.js` و`pdf.min.mjs` و`pdf.worker.min.mjs`، إلى مستودعك.
المراجعة اليدوية تعمل على GitHub Pages. خيارات API تحتاج تشغيل الخادم المحلي وفتح عنوانه على اللاب.
هذه النسخة لا تستضيف خادمًا عامًا ولا تضيف Plainer كتطبيق داخل ChatGPT.

## رفع الملفات واللغة

اختر ملف PDF أو Word بصيغة DOCX أو TXT، أو اسحبه إلى منطقة الملفات. الحد الأقصى ١٠ ميجابايت وملف واحد في المرة.
يُستخرج النص داخل المتصفح ويُضاف إلى المسودة الموجودة. إزالة اسم الملف لا تمسح النص.
ملفات DOC القديمة تحتاج حفظها بصيغة DOCX. ملفات PDF الممسوحة كصور تحتاج تحويل الصور إلى نص أولًا.
رفع PDF يحتاج فتح الموقع عبر الخادم المحلي أو GitHub Pages، وليس فتح HTML من القرص مباشرة.
اختيار اللغة محفوظ في المتصفح. تبديله يحافظ على المسودة ويعيدك إلى المحرر لبدء مراجعة باللغة المختارة.
الاستخدام بدون مفتاح يعتمد على النسخ واللصق وحدود حسابك في الخدمة؛ لا يرسل البرنامج الطلب تلقائيًا.

## فحص التعديلات

```powershell
npm run build
npm test
```

الاختبارات تستخدم ردودًا محاكية ولا تستهلك رصيد API.
